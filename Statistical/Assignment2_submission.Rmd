---
title: "Assignment2_submission"
author: "Gabor Wehrmuller - 219369109"
date: "07/09/2019"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
# Task 1 Source Weather Data



```{r}
# access the data
library(curl)

# new handle to set the token
h = new_handle()

handle_setheaders(h, "token" = "aeiJGRXtNXdMuIkEVeqOvciRvsdBbIUg")

# fetch the data
req_2 <- curl_fetch_memory("http://www.ncdc.noaa.gov/cdo-web/api/v2/data?datasetid=PRECIP_HLY&locationid=CITY:Perth,AU&startdate=2013-01-01&enddate=2014-01-01", handle = h)


# transform to dataframe to work with 
library(jsonlite)
jsonfile <- jsonlite::prettify(rawToChar(req_2$content))
df <- fromJSON(jsonfile)$results


```

